function between(a, b) {
  // your code here
  arr = [];
  for (i = a; i <= b; i++) {
    arr.push(i);
  }
  return arr;
}

//use an array to push in the params! this will give you the chance to loop
//through each number in the array!
